<UserControl x:Class="Demo_2310.UserControlEquipPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Demo_2310"
             mc:Ignorable="d" 
             d:DesignHeight="320" d:DesignWidth="1450">
    <Border BorderBrush="Black" 
        BorderThickness="1"
        Margin="5">
        <Grid Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320"/>
                <ColumnDefinition Width="900"/>
                <ColumnDefinition Width="200"/>
            </Grid.ColumnDefinitions>
            <!--Левый компонент-->
            <StackPanel Grid.Column="0"
                Margin="5">
                <Image Name="ImageS"
               MaxHeight="300"
               MaxWidth="300"/>
            </StackPanel>
            <!--Центральный компонент-->
            <StackPanel Grid.Column="1">
                <TextBlock FontWeight="Bold">
            <Run Text="{Binding Name}"/>
            <Run Text=" | "/>
            <Run Text="{Binding TypeName}"/>
                </TextBlock>
                <TextBlock>
            <Run Text="Производитель:"/>
            <Run Text="{Binding ProducerName}"/>
                </TextBlock>
                <TextBlock>
            <Run Text="Поставщик:"/>
            <Run Text="{Binding ProviderName}"/>
                </TextBlock>
                <StackPanel Orientation="Horizontal">
                    <TextBlock>
                <Run Text="Единица измерения:"/>
                <Run Text="{Binding UnitRent}"/>
                <Run Text="   "/>
                    </TextBlock>
                    <TextBlock  Name="BoxCount">
                        <Run Text="{Binding CountFree}"/>
                    </TextBlock>
                </StackPanel>
                <TextBlock TextWrapping="Wrap">
            <Run Text="Описание оборудования:"/>
            <Run Text="{Binding Description}"/>
                </TextBlock>
            </StackPanel>
            <!--Правый компонент-->
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="5*"/>
                </Grid.RowDefinitions>
                <Border Grid.Row="0" BorderBrush="Black" Margin="5" BorderThickness="1">
                    <TextBlock
                       FontSize="10"
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center">
                        <Run Text="Цена аренды:"/>
                        <Run Name="BoxPrice" Text="{Binding CostRent}"/>
                        <Run Name="BoxNewPrice"/>
                    </TextBlock>
                </Border>
                <Border Grid.Row="1" BorderBrush="Black" Margin="5" BorderThickness="1">
                    <TextBlock 
                       Name="TextDiscont"
                       FontSize="30"
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center">
                               <Run Text="{Binding Discount}"/>
                               <Run Text="%"/>
                    </TextBlock>
                </Border>
            </Grid>
        </Grid>
    </Border>
</UserControl>
